/**
 * Constants used throughout the application
 */

/**
 * Default thing codes for specific action types
 */
export const DEFAULT_THING_CODES = {
  /**
   * Thing code used for ProductionWaste activities
   * This is automatically assigned regardless of which product is selected
   */
  PRODUCTION_WASTE: "waste_municipal_landfill",
} as const;
